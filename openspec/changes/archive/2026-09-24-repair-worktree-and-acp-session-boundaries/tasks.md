## 1. Worktree session affiliation

- [x] 1.1 为会话创建与持久任务元数据增加源项目归属，保留运行路径作为工作区身份和命令 cwd。
- [x] 1.2 侧栏按源项目展示托管工作树会话，导航和归档继续使用原运行路径，并覆盖已有可核验工作树。
- [x] 1.3 加入创建、恢复、旧记录和同名不同仓库的行为测试。
- [x] 1.4 对齐创建前获取上游的设置、可快进基准选择、失败保护与会话内创建信息。

## 2. ACP session behavior

- [x] 2.1 从 Agent 宣告的模式生成新会话入口，并在首轮前校验和应用原生模式。
- [x] 2.2 将拒绝及失败元数据投影成错误终态和可读提示。
- [x] 2.3 在思考段后续正文、工具或计划更新时停止计时，确保转录重放一致。
- [x] 2.4 加入模式缺失/漂移、失败请求和思考续接测试。

## 3. Validation and review

- [x] 3.1 执行 OpenSpec validate、受影响测试、typecheck、lint 和架构检查。
- [x] 3.2 验证桌面项目侧栏、ACP 首发模式及失败/计时的可观察行为，执行一次 rv。（桌面已核对项目侧栏与无模式 Agent 的入口；有模式 Agent 的首发协议及失败/计时由定向测试验证，未取得真实第三方 Agent 的模式切换样本。）
